type Simp_gitlab::Ldap = Struct[{
  label                                   => String,
  host                                    => Simplib::Host,
  port                                    => Simplib::Port,
  uid                                     => String,
  bind_dn                                 => String,
  password                                => String,
  method                                  => Enum['simple','start_tls','simple_tls'],
  base                                    => String,
  Optional[allow_username_or_email_login] => Boolean,
  Optional[block_auto_created_users]      => Boolean,
  Optional[active_directory]              => Boolean,
  Optional[ca_file]                       => Stdlib::AbsolutePath,
  Optional[group_base]                    => String,
  Optional[user_filter]                   => String,
  Optional[ssl_version]                   => String,
  Optional[timeout]                       => Integer[0],
  Optional[verify_certificates]           => Boolean,
  Optional[attributes]                    => Hash,
  Optional[admin_group]                   => String,
  Optional[sync_ssh_keys]                 => Boolean,
}]
